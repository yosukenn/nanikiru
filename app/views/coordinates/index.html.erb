<div id="app">
  <navbar></navbar>
  <div class="container">
    <router-view @show="flowCoordinate($event)"></router-view>
    <modal coordinate="タイトル" v-if="showModal" @close="showModal = false">
  </div>
</div>

<%= javascript_pack_tag 'coordinate' %>
