<div class="user-from text-center p-5">
  <h2 class="mb-3">Please Log in</h2>
  <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: 'form-signin' }) do |f| %>
    <div class="field w-25 mx-auto">
      <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: 'Email address', class: 'form-control', id: 'inputEmail' %>
    </div>

    <div class="field w-25 mx-auto">
      <%= f.password_field :password, autocomplete: "current-password", placeholder: 'Password', class: 'form-control', id: 'inputPassword' %>
    </div>

    <% if devise_mapping.rememberable? -%>
      <div class="field">
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me %>
      </div>
    <% end -%>

    <div class="actions">
      <%= f.button "Log in", type: 'submit', class: 'btn btn-lg btn-primary btn-bloc' %>
    </div>
  <% end %>
  <%= render "devise/shared/links" %>
</div>
